using Gtk 4.0;

template $SearchButton: ToggleButton {
	icon-name: "system-search-symbolic";

	ShortcutController {
		scope: managed;

		Shortcut {
			trigger: bind $_get_trigger(template.keyboard_shortcut) as <Gtk.ShortcutTrigger>;
			action: "signal(start-searching)";
		}
	}
}
