using Gtk 4.0;
using Adw 1;

template $IgnitionWindow : Adw.ApplicationWindow {
	title: "Ignition";
	default-width: 600;
	default-height: 525;

	Adw.ToastOverlay toast_overlay {
		Stack stack {
			$FirstRunPage first_run_page {}
			$MainView main_view {}
		}
	}
}
